name: Reader Select range verification (temporary)

on:
  push:
    branches:
      - refactor/reader-selection-range

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - name: Show canonical Prettier diff
        run: |
          npx prettier --write src/reader/selection-range.ts
          git diff --unified=1 -- src/reader/selection-range.ts
          test -z "$(git status --short -- src/reader/selection-range.ts)"
