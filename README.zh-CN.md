# Zotero Neo

> **语言：** [English](README.md) · [Español](README.es-ES.md) · [中文](README.zh-CN.md)
>
> 基础项目：https://github.com/ZorroStardust/zotero-vim-plus
>
> 原始项目：https://codeberg.org/finktank/zotero-vim
>
> Zotero Neo 保留两个上游项目的版权与 attribution。

一个受 Neovim/LazyVim 启发、面向 Zotero 7–10 的键盘优先交互层。

![简短演示视频（无音频）](BriefDemoVideo.gif)

---

## 目录

- [功能](#功能)
- [系统要求](#系统要求)
- [安装](#安装)
- [从源码构建](#从源码构建)
- [模式](#模式)
- [默认按键绑定](#默认按键绑定)
  - [普通模式](#普通模式)
  - [光标模式](#光标模式)
  - [可视模式](#可视模式)
  - [插入模式](#插入模式)
- [标注工作流](#标注工作流)
- [待办事项](#待办事项)
- [自定义按键绑定](#自定义按键绑定)
- [设置](#设置)
- [架构说明](#架构说明)
- [许可证](#许可证)

---

## 功能

- **普通模式** — 滚动、翻页、在标注之间跳转、复制标注文本、删除标注、
  重定位视口（zt/zz/zb），以及放大时水平平移（`Shift+h`/`Shift+l`）
- **光标模式** — 像浏览器 Vim 插件一样移动文本光标，不选中文本
  （`hjkl`、`w/W`、`b/B`、`0/$`，以及 `2w` 等计数前缀）
- **可视模式** — 按行、字符、单词、句子或段落构建文本选择；创建彩色高亮或
  笔记；将选区或整个段落复制到剪贴板
- **插入模式** — 将所有按键暂时透传给 Zotero（在表单字段中输入时很有用）；
  选中标注时也会聚焦标注注释字段
- **完全可重映射** — 每个动作都可以从偏好设置面板重新绑定
- **无快捷键冲突** — vim 消费的按键会在 Zotero 阅读器的按键处理之前被拦截，
  因此 `l`（下一页）不会触发内置的朗读功能，`h`/`s`/`Ctrl+F` 也不会切换
  抓手工具 / 指针工具 / 查找栏
- **文本后处理** — 所有复制操作都会规范化 Unicode 连字（`ﬁ` → `fi` 等）
  并将 PDF 换行折叠为空格
- **快照与 EPUB 支持** — 滚动、搜索（`/` + `n`/`N`）、`gg`/`G`
  （顶部/底部）和 `zt`/`zz`/`zb` 在网页快照和 EPUB 中同样可用
  （快照没有页面，因此 `h`/`l` 会显示提示）。可视模式、光标模式
  和标注命令仍为 PDF 专属
- **标记（Marks）** — 类 Vim 位置标记（`m<x>` 设置、`` `<x> `` 瞬移跳转、
  `dm<x>`/`dM` 删除、`<space>m` 浏览浮层），字符可用 `a`–`z` 或 `0`–`9`
  （sioyek 风格数字标签）；可选持久化到父条目的 Extra 字段，重启后保留并同步

---

## 系统要求

- Zotero 7–10（插件使用 Zotero 7+ 的 Bootstrap API）
- macOS、Linux 或 Windows

---

## 安装

1. 从发布页面下载 `zotero-neo.xpi`（或自行构建——见下文）。
2. 打开 Zotero。
3. 进入 **工具 → 插件**。
4. 点击插件窗口右上角的**齿轮图标（⚙）**。
5. 选择**从文件安装插件…**并选中 `zotero-neo.xpi`。
6. 按提示重启 Zotero。

要更新，请用新的 `.xpi` 重复同样的步骤。Zotero 会自动替换旧版本。

---

## 从源码构建

```bash
git clone https://github.com/zhongyangchuwu/Zotero-Neo.git
cd Zotero-Neo
./build.sh
```

`build.sh` 将插件源码打包为 `zotero-neo.xpi`。无需构建工具或包管理器——
只需要 `zip`（macOS 和大多数 Linux 发行版默认自带）。如果 `node` 可用，
该脚本还会校验 JS 文件语法以及按键绑定表（zoteroVim.js 与 prefs.js）
是否保持同步。

```
Zotero-Neo/
├── manifest.json          插件清单（ID、版本、Zotero 版本范围）
├── bootstrap.js           生命周期钩子（启动/关闭/窗口事件）
├── build.sh               构建 zotero-neo.xpi（含完整性检查）
├── content/
│   ├── zoteroVim.js       核心：模式、按键处理、动作分发器
│   ├── zoteroVimReader.js 阅读器侧方法（目录、可视/光标、标注）
│   ├── zoteroVimMain.js   主窗口方法（笔记编辑器、选择器、笔记布局）
│   ├── preferences.xhtml  偏好设置面板 UI（XUL/HTML 混合）
│   └── prefs.js           偏好设置面板 JS（读写 Firefox 偏好）
├── tools/
│   └── check-sync.js      校验按键绑定表保持同步
└── icons/
    ├── icon-64x64.png     插件图标（偏好设置面板、清单）
    ├── icon-128x128.png   插件图标（清单）
    └── zotero-neo.svg     标志的矢量源文件
```

---

## 模式

插件在四种模式下工作，显示在 PDF 阅读器右下角的小浮层中：

| 模式 | 指示器 | 用途 |
|------|-----------|---------|
| **普通模式** | *(隐藏)* | 默认——导航和标注命令 |
| **光标模式** | `-- CURSOR --` | 无文本选择的光标导航 |
| **可视模式** | `-- VISUAL --` | 文本选择与标注创建 |
| **插入模式** | `-- INSERT --` | 透传——所有按键都交给 Zotero |

模式转换：

```
Normal ──c──▶ Cursor ──Escape────▶ Normal
Normal ──v──▶ Visual ──v/Escape──▶ Normal
Normal ──i──▶ Insert ──Escape────▶ Normal
Cursor ──v──▶ Visual ──v/Escape──▶ Normal
```

---

## 默认按键绑定

### 普通模式

#### 滚动

| 按键 | 动作 |
|-----|--------|
| `j` | 向下滚动 |
| `k` | 向上滚动 |
| `Shift+h` (`H`) | 向左滚动 |
| `Shift+l` (`L`) | 向右滚动 |
| `Ctrl+d` | 向下半页 |
| `Ctrl+u` | 向上半页 |
| `Ctrl+f` | 向下整页 |
| `Ctrl+b` | 向上整页 |

计数前缀会成倍增加步长——`3j` 滚动三步，`2ctrl+f` 滚动两个整页，
以此类推。

#### 页面导航

| 按键 | 动作 |
|-----|--------|
| `h` | 上一页 |
| `l` | 下一页 |
| `gg` | 第一页 |
| `G` | 最后一页 |
| `Shift+J` (`J`) | 切换到上一个打开的标签页 |
| `Shift+K` (`K`) | 切换到下一个打开的标签页 |
| `<space>bj` | 打开标签选择器（基于提示字母跳转到打开的标签页） |
| `<space>n` | 打开笔记布局浮层（左侧：笔记标题列表，右侧：笔记预览） |

计数前缀会重复翻页（`3l` = 向前三页），带计数的 `gg`/`G` 会跳转到该页码
（`5G` / `5gg` = 第 5 页）。

> **注意：** Zotero 内置的朗读功能也会监听 `l`/`r` 键。插件会拦截 Zotero
> 阅读器对 vim 所消费按键的转发，因此 `l`（下一页）永远不会触发朗读。
> 要使用朗读，请按 `r`（vim 默认未绑定）或点击朗读工具栏按钮。

#### 模糊选择器

| 按键 | 动作 |
|-----|--------|
| `<space>ff` | 对当前库中的所有条目打开模糊选择器 |
| `<space>fb` | 对当前分类中的条目打开模糊选择器 |
| `<space>bj` | 打开标签选择器（见下文） |

选择器会在屏幕中央打开一个搜索框。开始输入即可筛选条目（顺序模糊匹配——
每个字符必须按顺序出现）。然后：

| 按键 | 动作 |
|-----|--------|
| `↑` / `↓`（或 `Ctrl+n` / `Ctrl+p`、`Ctrl+j` / `Ctrl+k`） | 上 / 下移动选择 |
| `Enter` | 在条目列表中选择该条目 / 跳转到所选标签页 |
| `Ctrl+o` | 打开所选条目的 PDF（仅条目选择器） |
| `y` | 将所选条目的完整引文复制到剪贴板 |
| `yy` | 将所选条目的 citekey 复制到剪贴板 |
| `Escape` | 关闭选择器 |

**标签选择器**（`<space>bj`）对当前打开的标签页使用相同的界面，并额外提供
**提示字母**：每个标签页显示一个字母标签，输入其字母即可直接选择该标签页
（根据标签页数量使用单字母或双字母提示）。`y`/`yy` 复制在标签选择器中不可用。

#### 笔记布局浮层

左侧显示笔记条目（仅标题），分为当前条目笔记和全部笔记两组。
右侧显示当前选中笔记的预览内容。如果当前标签页是 PDF 阅读器，
从该浮层打开笔记时会优先使用 Zotero 的右侧笔记编辑器，以便并排阅读和编辑。

| 按键 | 动作 |
|-----|--------|
| `j` / `k` | 笔记焦点下 / 上移 |
| `Ctrl+d` / `Ctrl+u` | 在笔记列表中快速下 / 上移 |
| `Ctrl+j` / `Ctrl+k` | 在左侧列表的当前条目笔记与全部笔记分区之间切换 |
| `Ctrl+l` | 将焦点移到右侧预览面板 |
| `Ctrl+h` | 将焦点移回左侧笔记列表 |
| `n` | 在所选笔记的父条目下创建新子笔记，并在右侧笔记编辑器中打开 |
| `Shift+N` | 在当前选中条目下创建新子笔记，并在新笔记标签页中打开 |
| `gg` / `G` | 跳到浮层中的第一个 / 最后一个笔记 |
| 提示字母 | 通过提示标签快速聚焦（单键或双键） |
| `Enter` | 在右侧笔记编辑器中打开所选笔记（优先阅读器标签页） |
| `Shift+Enter` | 在新笔记标签页中打开所选笔记 |
| `Escape` | 关闭笔记布局浮层 |

#### 目录浏览

| 按键 | 动作 |
|-----|--------|
| `<space>e` | 切换自定义目录浏览浮层 |
| `j` / `k` | 目录选择下 / 上移 |
| `Ctrl+d` / `Ctrl+u` | 快速下 / 上移 |
| `l` | 展开所选目录节点 |
| `h` | 折叠所选目录节点 |
| `R` / `M` | 全部展开 / 全部折叠目录节点 |
| `gg` / `G` | 跳到顶部 / 底部目录条目 |
| 提示字母 | 选择提示的目录条目而不跳转 |
| `Enter` | 跳转到所选目录条目并返回普通模式 |
| `Escape` | 关闭目录浏览 |

目录浏览打开时会尝试预选离当前阅读位置最近的目录条目；如果 PDF 元数据
无法可靠映射，则回退到第一个可见目录条目。每个可见条目也会显示提示标签。
条目数量少时提示为单字符，否则扩展为双字符提示。输入提示只改变当前选择；
仍需按 `Enter` 跳转。

#### 阅读器分栏视图

| 按键 | 动作 |
|-----|--------|
| `<space>-` | 切换水平分栏（上/下） |
| `<space>\|` | 切换垂直分栏（左/右） |
| `Ctrl+h` | 从右侧笔记编辑器返回活动阅读器面板；否则聚焦左侧分栏面板 |
| `Ctrl+j` | 聚焦下方分栏面板（垂直分栏中则切换面板） |
| `Ctrl+k` | 聚焦上方分栏面板（垂直分栏中则切换面板） |
| `Ctrl+l` | 垂直分栏中，先移动到右侧阅读器面板，再到右侧笔记编辑器；否则聚焦右侧分栏面板 |

#### 标记（Marks）

类 Vim 的位置标记，用于快速跳转。`m<x>` 在当前视口位置设置标记，
`` `<x> `` 跳回该位置。标记字符可用 `a`–`z` 或 `0`–`9`（sioyek 风格数字标签）。

| 按键 | 动作 |
|-----|--------|
| `m<x>` | 在当前位置设置标记（如 `ma`、`m1`） |
| `` `<x> `` | 跳转到标记 —— 瞬移翻页，精确还原打标记时的画面（如 `` `a ``、`` `1 ``） |
| `dm<x>` | 删除标记（如 `dma`） |
| `dM` | 删除全部标记 |
| `<space>m` | 切换标记浏览浮层（输入标记字符直接跳转；`j`/`k` 移动、`Enter` 跳转、`d` 删除、`x` 删除全部） |

说明：

- 标记前缀后的数字是标记字符而非计数 —— `4j` 仍然滚动四步，但 `` `1 ``
  跳转到标记 1。
- 标记记录的是**视口中央**的位置：打标记时屏幕中央的内容，跳转回来后仍在
  屏幕中央 —— 即使是在页面中部也精确还原。
- 如果选中了标注（通过 `[`/`]`），标记也会绑定到该标注，跳转后 `[`/`]`、
  `zy`、`y` 等后续命令照常可用。`[`/`]` 跳转注释不受影响。
- 启用 **持久化标记**（偏好设置 → 标记）后，全部标记会保存为**父条目 Extra
  字段**中的 `zv-marks-<附件key>:` 行（随 Zotero 同步——Zotero 9 的附件没有
  Extra 字段，因此存在父条目上；一个条目下的多个 PDF 各自独立成行），失败
  时回退到本机 pref。状态栏会显示实际使用的后端（`· saved (extra)` /
  `· saved (local)`）。旧注释标签方案的标记会自动迁移。
- 未启用持久化时设置的标记仅在当前阅读器会话内有效。

#### 笔记编辑器（侧栏面板和独立笔记标签页）

当 Zotero 笔记编辑器获得焦点（右侧栏面板或独立笔记标签页）时，
插件提供一层精简的类 Vim 操作。

| 按键 | 动作 |
|-----|--------|
| `i` | 进入笔记插入模式（透传输入） |
| `a` / `A` / `I` | 在下一字符 / 行尾 / 行首进入插入模式 |
| `o` / `O` | 在下方 / 上方打开新行并进入插入模式 |
| `Escape` | 返回笔记普通模式 |
| `h` / `l` | 光标左 / 右移 |
| `j` / `k` | 光标下 / 上移行 |
| `w` / `e` / `b` | 按单词移动（前进到词首 / 词尾 / 后退） |
| `W` / `E` / `B` | 大单词变体 |
| `0` / `^` / `$` | 移动到行首 / 第一个非空白字符（近似）/ 行尾 |
| `gg` | 跳到第一行 |
| `G` | 跳到最后一行 |
| `3j`（示例） | 动作的计数前缀（重复 3 次） |
| `3G` / `12gg` | 计数前缀跳转到特定行号 |
| `x` | 删除光标处的字符 |
| `dd` | 删除当前行 |
| `yy` | 将当前行复制到剪贴板 |
| `dw` / `de` / `db` / `d$` | 按动作删除（单词 / 词尾 / 向后单词 / 到行尾） |
| `yw` / `ye` / `yb` / `y$` | 按动作复制 |
| `cw` / `ce` / `c$` | 按动作修改（删除范围并进入插入模式） |
| `diw` / `yiw` / `ciw` | 词内文本对象（删除 / 复制 / 修改） |
| `p` / `P` | 在光标后 / 前粘贴上次复制或删除的文本 |
| `u` / `Ctrl+r` | 撤销 / 重做桥接 |
| `<space>...` | 主窗口 leader 绑定在笔记普通模式中可用（例如 `<space>n`、`<space>ff`） |
| `Shift+J` / `Shift+K` | 从笔记普通模式切换到上一个 / 下一个标签页 |

`dd`、`yy` 和 `x` 支持计数前缀（例如 `3dd`、`5yy`、`4x`）。操作符 + 动作
组合也支持计数（例如 `3dw`、`2y$`）。`p` 和 `P` 使用插件内部的笔记寄存器
（由 `yy` 和 `dd` 更新）。

#### 库树导航（左栏）

这些绑定作用于 Zotero 原生左栏（分类树和条目列表），当该栏获得焦点时生效。

| 按键 | 动作 |
|-----|--------|
| `j` / `k` | 选择下 / 上移（分类树和条目列表） |
| `gg` / `G` | 跳到第一 / 最后一行 |
| `h` | 在条目列表中，将焦点移回分类树；在分类树中，折叠所选分类或跳转到父级 |
| `l` | 在分类树中，展开所选分类；如果已展开或为叶节点，将焦点移入条目列表 |
| `Enter` | 在分类树中，将焦点移入条目列表；在条目列表中，打开所选条目/PDF |
| `Backspace` | 跳转到父分类 |
| `za` | 切换所选分类行的展开 / 折叠 |
| `zo` | 展开当前分类行（如果已打开则保持打开） |
| `zc` | 折叠当前分类行（如果已关闭则保持关闭） |
| `R` | 展开当前库树中的所有分类 |
| `M` | 折叠当前库树中的所有分类 |

#### 主窗口 `<space>` 组合键

这些绑定在主 Zotero 窗口（而非阅读器内）对当前焦点生效：

| 按键 | 动作 |
|-----|--------|
| `<space>ff` | 对当前库中的所有条目打开模糊选择器 |
| `<space>fb` | 对当前分类中的条目打开模糊选择器 |
| `<space>bj` | 打开标签选择器 |
| `<space>n` | 切换笔记布局浮层 |
| `<space>e` | 聚焦分类树 |
| `<space>yy` | 将所选条目的 citekey 复制到剪贴板 |
| `<space>o` | 打开所选条目的 PDF |
| `<space>q` | 关闭活动的 PDF 标签页 |
| `<space>/` | 聚焦 Zotero 的搜索栏 |
| `<space>wh` | 聚焦分类树（左栏） |
| `<space>wl` | 聚焦详情面板（右栏） |
| `<space>ww` | 聚焦条目列表（中间栏） |

#### 视口定位（类似 Vim 的 z 命令）

| 按键 | 动作 |
|-----|--------|
| `zt` | 滚动使当前页面位于视图**顶部** |
| `zz` | 滚动使当前页面位于视图**中央** |
| `zb` | 滚动使当前页面位于视图**底部** |

#### 搜索

| 按键 | 动作 |
|-----|--------|
| `/` | 打开 PDF 查找栏 |
| `n` | 跳转到下一个搜索结果 |
| `N` | 跳转到上一个搜索结果 |
| `Escape` | 清除 / 关闭搜索 |

搜索的工作方式与 Vim 类似：按 `/` 打开 Zotero 的查找栏，输入查询
（结果会随输入实时高亮），然后按 `Enter`——焦点会自动返回 PDF，
`n` / `N` 在匹配项之间循环，同时结果计数器保持可见。再次按 `/`
会以之前选中的查询重新打开查找栏。`Escape` 关闭查找栏并清除高亮。
没有活动搜索时按 `n` / `N` 会显示先进行搜索的提示。

#### 按颜色筛选侧边栏

| 按键 | 动作 |
|-----|--------|
| `Zy` | 筛选侧边栏 → 仅黄色标注 |
| `Zr` | 筛选侧边栏 → 仅红色标注 |
| `Zg` | 筛选侧边栏 → 仅绿色标注 |
| `Zb` | 筛选侧边栏 → 仅蓝色标注 |
| `Zp` | 筛选侧边栏 → 仅紫色标注 |
| `Za` | 清除颜色筛选（显示所有标注） |

> **提示：** `z`（小写）作用于标注（改色）。`Z`（大写）作用于侧边栏视图（筛选）。

#### 标注导航与编辑

使用 `[` 和 `]` 在标注之间移动。选中的标注会在 PDF 中高亮，
并在侧边栏中滚动到对应卡片。

| 按键 | 动作 |
|-----|--------|
| `[` | 跳到上一个标注 |
| `]` | 跳到下一个标注 |
| `Enter` | 打开所选标注的注释字段进行编辑 |
| `i` | 进入插入模式**并**聚焦标注注释字段 |
| `y` | 将标注的**高亮文本**复制到剪贴板 |
| `yy` | 将标注的**注释文本**复制到剪贴板 |
| `dd` | 删除所选标注 |
| `zy` | 将标注颜色改为黄色 |
| `zr` | 将标注颜色改为红色 |
| `zg` | 将标注颜色改为绿色 |
| `zb` | 将标注颜色改为蓝色 |
| `zp` | 将标注颜色改为紫色 |

> **提示：** `y` 与 `yy`——插件在触发单 `y` 动作前会等待最多 800 ms
> 的第二下 `y`。快速输入 `yy` 总是生效。

#### 模式切换

| 按键 | 动作 |
|-----|--------|
| `v` | 进入可视模式 |
| `c` | 进入光标模式 |
| `i` | 进入插入模式 |

---

### 光标模式

从普通模式按 `c` 进入光标模式。按下 `c` 后，插件会在视口中候选文本位置
显示提示徽标（与可视模式相同的视觉样式）。按提示字母将光标放置到该位置。

#### 光标移动

| 按键 | 动作 |
|-----|--------|
| `j` / `k` | 光标按可视行下 / 上移 |
| `h` / `l` | 光标按字符左 / 右移 |
| `w` | 光标向前移动一个单词 |
| `W` | 光标向前移动一个 WORD（非空白块） |
| `b` | 光标向后移动一个单词 |
| `B` | 光标向后移动一个 WORD（非空白块） |
| `0` / `$` | 光标移动到行首 / 行尾 |
| `2w`、`3b`、... | 计数前缀重复动作 |

#### 模式切换

| 按键 | 动作 |
|-----|--------|
| `a..z`（提示） | 将光标放置到所选提示文本位置 |
| `v` | 从当前光标位置进入可视模式 |
| `Escape` | 退出到普通模式 |

---

### 可视模式

从普通模式按 `v` 进入可视模式。如果没有已有文本选择，插件会在可见页面上
显示**提示徽标**（黄色字母标签）于句首。按对应字母将选择锚定在该位置。
之后选择会随按动动作键而扩展。

#### 扩展选择

| 按键 | 动作 |
|-----|--------|
| `j` / `k` | 选择向下 / 上扩展一行 |
| `h` / `l` | 选择向左 / 右扩展一个字符 |
| `w` / `b` | 选择向前 / 后扩展一个单词 |
| `0` / `$` | 选择扩展到行首 / 行尾 |
| `)` / `(` | 选择扩展到下一个 / 上一个句首 |
| `}` / `{` | 选择扩展到段落结尾 / 开头 |
| `o` | **交换锚点与焦点**——跳到选择的另一端（类似 Vim 可视模式下的 `o`）；后续动作键从新的一端继续扩展 |

#### 创建标注

| 按键 | 动作 |
|-----|--------|
| `zy` | 创建**黄色**高亮 |
| `zr` | 创建**红色**高亮 |
| `zg` | 创建**绿色**高亮 |
| `zb` | 创建**蓝色**高亮 |
| `zp` | 创建**紫色**高亮 |
| `za` | 添加**笔记**标注（创建高亮并打开注释编辑器） |
| `i` | 与 `za` 相同（快速笔记并进入注释插入模式） |

#### 复制文本

| 按键 | 动作 |
|-----|--------|
| `y` | 将**当前选择**复制到剪贴板 |
| `yy` | 将包含选择的**整个段落**复制到剪贴板 |
| `#` | 打开查找栏并搜索**当前选择** |

所有复制操作都会应用 Unicode NFKC 规范化（解析 `ﬁ` → `fi` 等连字）
并将 PDF 换行折叠为空格。

#### 退出可视模式

| 按键 | 动作 |
|-----|--------|
| `v` | 退出到普通模式（清除选择） |
| `Escape` | 退出到普通模式（清除选择） |

---

### 插入模式

在插入模式下，每个按键都会原样透传给 Zotero。当你需要向 Zotero 自身的
UI 元素输入内容而无需 vim 绑定拦截按键时，这很有用。

当在普通模式下选中标注（通过 `[`/`]`）时按 `i`，插件会自动进入插入模式
**并**聚焦标注的注释字段，以便立即开始输入。按 `Escape` 保存并返回普通模式。

| 按键 | 动作 |
|-----|--------|
| `Escape` | 退出插入模式 → 普通模式 |

---

## 标注工作流

### 从零创建高亮

1. 按 `v` 进入可视模式。
2. 按目标句首显示的提示字母（或按 `j`/`k` 从当前位置开始）。
3. 用 `j`/`k`/`w`/`b`/`)`/`}`/`h`/`l` 扩展选择。
4. 如需修剪起点而不是扩展终点，用 `o` 跳到选择的另一端。
5. 按 `zy`/`zr`/`zg`/`zb`/`zp` 创建彩色高亮，或按 `za` 添加笔记。

### 导航与编辑已有标注

1. 按 `]` / `[` 移动到下一个 / 上一个标注。标注会在 PDF 阅读器中高亮，
   侧边栏会滚动到其卡片。
2. 按 `y` 复制高亮文本，`yy` 复制注释。
3. 按 `i`（或 `Enter`）打开注释字段并输入笔记。按 `Escape` 返回普通模式。
4. 按 `dd` 删除标注。

---

## 待办事项

- [ ] 原生阅读器侧边栏集成：直接集成 Zotero/PDF.js 内置的左侧边栏。
   自定义目录浏览浮层已可用，但原生侧边栏工作流仍被推迟，
   因为其焦点/DOM 行为不够稳定。

---

## 自定义按键绑定

打开 **编辑 → 偏好设置**（macOS：**Zotero → 设置**），
进入 **Zotero Neo** 标签页。

- **按键绑定**表中的每一行都将一个 *模式 + 键序列* 映射到一个 *动作*。
- 点击键序列单元格可直接编辑。
- 使用小写字母。Ctrl 用 `ctrl+` 前缀（macOS 上为 Cmd）。
- 支持 `gg`、`zy`、`yy` 等多键序列。
- 点击 **+ 添加绑定** 添加新行；点击 **×** 删除一行。
- 点击 **应用绑定** 保存按键绑定更改。
- 高亮颜色、模式、标记与滚动设置在更改时自动保存。
- 笔记编辑器 Vim 模式可以从偏好设置面板独立开启或关闭。
- 点击 **重置为默认值** 将所有绑定恢复为默认值。

偏好设置面板使用稳定的面板 id 注册，因此即使重启后也会直接定位到上次
停留的设置区块；下拉框采用 Zotero 原生的 `menulist` 控件，确保每次打开
都稳定可用。面板初始化异常会以 `[prefs]` 前缀记录到配置文件目录下的
`zotero-neo-startup.log`。

### 动作参考

| 动作 | 说明 |
|--------|-------------|
| `scrollDown` | 按配置的步长向下滚动 |
| `scrollUp` | 按配置的步长向上滚动 |
| `scrollLeft` | 按配置的步长向左滚动 |
| `scrollRight` | 按配置的步长向右滚动 |
| `halfPageDown` | 向下滚动半个视口 |
| `halfPageUp` | 向上滚动半个视口 |
| `fullPageDown` | 向下滚动一个完整视口 |
| `fullPageUp` | 向上滚动一个完整视口 |
| `scrollTop` | 重新定位视图使当前页面位于顶部 |
| `scrollCenter` | 重新定位视图使当前页面居中 |
| `scrollBottom` | 重新定位视图使当前页面位于底部 |
| `prevPage` | 上一页 |
| `nextPage` | 下一页 |
| `firstPage` | 第一页 |
| `lastPage` | 最后一页 |
| `openSearch` | 打开查找栏 |
| `findNext` | 跳转到下一个搜索结果 |
| `findPrevious` | 跳转到上一个搜索结果 |
| `clearSearch` | 关闭 / 清除查找栏 |
| `prevAnnotation` | 跳到上一个标注 |
| `nextAnnotation` | 跳到下一个标注 |
| `editAnnotation` | 聚焦标注注释字段（Enter） |
| `deleteAnnotation` | 删除所选标注 |
| `filterYellow` | 将侧边栏筛选为仅黄色标注 |
| `filterRed` | 将侧边栏筛选为仅红色标注 |
| `filterGreen` | 将侧边栏筛选为仅绿色标注 |
| `filterBlue` | 将侧边栏筛选为仅蓝色标注 |
| `filterPurple` | 将侧边栏筛选为仅紫色标注 |
| `filterClear` | 清除颜色筛选（显示所有标注） |
| `recolorYellow` | 将所选标注颜色改为黄色 |
| `recolorRed` | 将所选标注颜色改为红色 |
| `recolorGreen` | 将所选标注颜色改为绿色 |
| `recolorBlue` | 将所选标注颜色改为蓝色 |
| `recolorPurple` | 将所选标注颜色改为紫色 |
| `yankAnnotation` | 复制标注高亮文本 |
| `yankAnnotationComment` | 复制标注注释文本 |
| `enterVisual` | 进入可视模式 |
| `enterCursor` | 进入光标模式 |
| `enterInsert` | 进入插入模式（选中标注时同时聚焦注释） |
| `exitMode` | 返回普通模式 |
| `extendDown` | 选择向下扩展一行 |
| `extendUp` | 选择向上扩展一行 |
| `extendLeft` | 选择向左扩展一个字符 |
| `extendRight` | 选择向右扩展一个字符 |
| `extendWordForward` | 选择扩展到下一个单词 |
| `extendWordBackward` | 选择扩展到上一个单词 |
| `extendLineStart` | 选择扩展到当前行行首 |
| `extendLineEnd` | 选择扩展到当前行行尾 |
| `extendSentenceForward` | 选择扩展到下一个句首 |
| `extendSentenceBackward` | 选择扩展到上一个句首 |
| `extendParagraphForward` | 选择扩展到当前段落结尾 |
| `extendParagraphBackward` | 选择扩展到当前段落开头 |
| `highlightYellow` | 创建黄色高亮 |
| `highlightRed` | 创建红色高亮 |
| `highlightGreen` | 创建绿色高亮 |
| `highlightBlue` | 创建蓝色高亮 |
| `highlightPurple` | 创建紫色高亮 |
| `addNote` | 添加笔记标注 |
| `copySelection` | 将当前选择复制到剪贴板 |
| `searchSelection` | 打开查找栏并搜索当前选择 |
| `yankParagraph` | 将整个段落复制到剪贴板 |
| `swapVisualEnds` | 交换选择锚点与焦点 |
| `cursorDown` | 光标按可视行下移（光标模式） |
| `cursorUp` | 光标按可视行上移（光标模式） |
| `cursorLeft` | 光标按字符左移（光标模式） |
| `cursorRight` | 光标按字符右移（光标模式） |
| `cursorWordForward` | 光标向前移动一个单词（光标模式） |
| `cursorBigWordForward` | 光标向前移动一个 WORD（光标模式） |
| `cursorWordBackward` | 光标向后移动一个单词（光标模式） |
| `cursorBigWordBackward` | 光标向后移动一个 WORD（光标模式） |
| `cursorLineStart` | 光标移动到行首（光标模式） |
| `cursorLineEnd` | 光标移动到行尾（光标模式） |
| `cursorToVisual` | 从当前光标位置进入可视模式 |
| `mainTabPick` | 打开当前打开的 Zotero 标签页的标签选择器 |
| `mainNotesLayout` | 切换笔记布局浮层（左列表 + 右预览） |
| `mainFuzzyAll` | 对当前库中的所有条目打开模糊选择器 |
| `mainFuzzyCollection` | 对当前分类中的条目打开模糊选择器 |
| `mainYankCitekey` | 将所选条目的 citekey 复制到剪贴板 |
| `mainOpenPDF` | 打开所选条目的 PDF |
| `mainClosePDF` | 关闭活动的 PDF 标签页 |
| `mainPrevTab` | 切换到上一个打开的标签页 |
| `mainNextTab` | 切换到下一个打开的标签页 |
| `mainFocusTree` | 聚焦分类树（左栏） |
| `mainFocusItems` | 聚焦条目列表（中间栏） |
| `mainFocusLeft` | 聚焦分类树（左栏） |
| `mainFocusRight` | 聚焦详情面板（右栏） |
| `mainFocusSearch` | 聚焦 Zotero 的搜索栏 |
| `mainNavDown` | 选择下移（分类树 / 条目列表） |
| `mainNavUp` | 选择上移（分类树 / 条目列表） |
| `mainNavFirst` | 跳到第一行 |
| `mainNavLast` | 跳到最后一行 |
| `toggleReaderSidebarOutline` | 切换自定义目录浏览浮层 |
| `focusReaderSidebar` | 聚焦或重新打开自定义目录浏览浮层 |
| `toggleReaderSplitHorizontal` | 切换阅读器水平分栏视图 |
| `toggleReaderSplitVertical` | 切换阅读器垂直分栏视图 |
| `focusReaderSplitLeft` | 聚焦左侧分栏面板（水平分栏中则切换） |
| `focusReaderSplitDown` | 聚焦下方分栏面板（垂直分栏中则切换） |
| `focusReaderSplitUp` | 聚焦上方分栏面板（垂直分栏中则切换） |
| `focusReaderSplitRight` | 聚焦右侧分栏面板（水平分栏中则切换） |
| `mainActivate` | 在分类中进入条目列表；在条目中打开所选条目/PDF |
| `mainTreeToggle` | 切换所选分类的展开 / 折叠 |
| `mainTreeOpenOnly` | 展开所选分类而不改变面板 |
| `mainTreeCloseOnly` | 折叠所选分类而不移到父级 |
| `mainTreeExpand` | 展开所选分类或将焦点移入条目列表 |
| `mainTreeCollapse` | 折叠所选分类、移到父级或将焦点返回分类树 |
| `mainTreeParent` | 选择移到父分类 |
| `mainTreeExpandAll` | 展开左树中的所有分类 |
| `mainTreeCollapseAll` | 折叠左树中的所有分类 |

---

## 设置

| 设置项 | 默认值 | 说明 |
|---------|---------|-------------|
| 启用可视模式 | 开 | 允许用 `v` 进入可视模式 |
| 启用光标模式 | 开 | 允许用 `c` 进入光标模式 |
| 启用插入模式 | 开 | 允许用 `i` 进入插入模式 |
| 笔记编辑器 Vim 模式 | 开 | 在笔记编辑器中启用类 Vim 编辑（侧栏面板和笔记标签页） |
| 滚动模式 | 匀速跟随 | 步进 / 匀速跟随 / 梯形加速 — 仅显示当前模式的参数 |
| 滚动步长 | 60 px | 每次按 `j`/`k`/`H`/`L` 滚动的像素数（步进模式；`3j` 等计数前缀始终使用它） |
| 滚动速度 | 2000 px/s | 按住时的恒定滚动速度（匀速跟随模式） |
| 平滑初始速度 | 2000 px/s | 按住式平滑滚动的起始速度（梯形加速模式） |
| 平滑最大速度 | 2000 px/s | 最大按住滚动速度（梯形加速模式） |
| 平滑加速度 | 2600 px/s² | 按住滚动键时的速度增量（梯形加速模式） |
| 平滑减速度 | 4200 px/s² | 松开按键后的速度衰减（梯形加速模式） |
| 松开即停 | 关 | 启用后松开按键立即停止（梯形加速模式） |
| 持久化标记 | 关 | 将标记保存到父条目的 Extra 字段（`zv-marks-<attachmentKey>`），重启后保留并同步 |
| 默认高亮颜色 | 黄色 | 未按显式颜色键时使用的颜色 |

滚动设置在更改时自动保存。

- **步进模式**：每次按 `j`/`k`/`H`/`L` 立即滚动一个步长。
- **匀速跟随模式**：按住滚动键时以固定速度平滑滚动，松开立即停止。
- **梯形加速模式**：按住时从 `初始速度` 加速到 `最大速度`，松开后按
  `减速度` 滑行停止（除非勾选 *松开即停*）。当 `初始速度` 与 `最大速度`
  都设为 `2000` 时，手感接近匀速模式，但松开后带轻微滑行。

---

## 架构说明

这些说明面向贡献者或任何调试插件的人。

### 三层 iframe 栈

Zotero 的 PDF 阅读器渲染在嵌套的 iframe 中：

```
Zotero chrome window
  └─ reader.html          (reader._iframeWindow)
       └─ PDF.js iframe   (reader._internalReader._primaryView._iframeWindow)
```

按键事件在最内层（PDF.js）通过 `pdfWin.addEventListener` 注册的
`capture: true` 的 `keydown` 监听器捕获。

### Cross-Compartment 安全

`reader._internalReader` 和 PDF.js 查看器对象位于与 Zotero chrome 上下文
不同的 JavaScript 安全域（compartment）。任何跨此边界作为参数传递的
JavaScript 对象或数组都必须先克隆：

```js
Components.utils.cloneInto(value, targetWindow)
```

基本类型值（数字、字符串、布尔值）可以自由跨域。忘记 `cloneInto` 会产生
`"Permission denied to access property"` 错误，这类错误容易被忽略，
因为它们常常被捕获并静默吞掉。

### Zotero 内置快捷键冲突（朗读 Read Aloud）

Zotero 阅读器 React 应用在 `l`/`r` 键上启动朗读，并从其 `KeyboardManager`
在 `h`/`s` 上切换抓手工具 / 指针工具。在 PDF.js iframe 内按下的键通过
**直接 JS 调用**到达它——`view._onKeyDown(event)`（在阅读器的
`pdf-view.js` 中）——而不是通过 DOM 事件传播。这意味着：

- PDF.js iframe 内的 `preventDefault()` / `stopPropagation()` 无法阻止它：
  Zotero 的捕获监听器与插件的监听器运行在*同一*窗口上，而转发是普通函数
  调用，不是事件分发。
- 原生 keydown 事件永远不会跨 iframe 边界进入 reader.html，因此那里的
  文档级拦截器永远看不到 PDF 按键。

因此插件会修补转发回调本身：

- `_patchReaderKeyForwarding()` 用包装器替换 PdfView 实例
  （`_internalReader._primaryView` / `_secondaryView`）上的 `_onKeyDown`，
  当 `_readerConsumesKey()` 表示 vim 将处理该键时丢弃该事件（当前模式的
  精确绑定或前缀）。插入模式除 Escape 外全部透传。
- 包装器通过 `Cu.unwrap` + `Cu.exportFunction` 跨 chrome/content 边界
  （裸的 chrome 函数会被 content 域拒绝）。
- 无论 vim 在何处消费按键，`_onKeyDown` 都额外使用
  `stopImmediatePropagation()`（而非 `stopPropagation()`），因此当插件的
  监听器先注册时，Zotero 的同窗口捕获监听器会被跳过。

该修补由 250 ms 的视图同步计时器重新应用，因此可跨视图重建、分栏视图
和恢复会话存活。如果 Zotero 更改阅读器的转发内部实现，请重新验证
`view._onKeyDown` 和 `KeyboardManager` 快捷键表（keyboard-manager.js）。

### 标注导航

`reader._internalReader.setSelectedAnnotations(Cu.cloneInto([key], readerWin))`
是处理所有事情的单次调用——它会滚动 PDF 到标注、显示选中轮廓，
并滚动侧边栏卡片进入视野。**不要**再调用 `currentPageNumber = N` 或
`scrollPageIntoView`；它们会与内部导航竞争并导致跳跃式的翻页。

### 可视模式下的文本选择

PDF.js 将每个可视行渲染为 `.textLayer` 元素中绝对定位的 `<span>`
（每页一个 `.textLayer`）。`Selection.modify('extend', 'line')` 等浏览器
API 在此上下文中不可靠。

插件实现了自己的按行扩展（`_extendByLine`），使用
`document.caretPositionFromPoint` 和回退的 span 几何扫描。句子和段落扩展
（`_extendBySentence`、`_extendByParagraph`）使用
`document.querySelectorAll('.textLayer span')` 扫描*所有页面*的
`.textLayer span` 元素——**不是** `document.querySelector('.textLayer')`，
后者只返回第一页。

选择通过 `state.visualCursor = { textNode, offset }` 作为锚点跟踪，
以便 PDF.js 偶尔清除浏览器选择时 `sel.addRange()` 能重建正确的范围。

### 文本后处理（yank 操作）

所有剪贴板操作都会将原始的 `sel.toString()` 或 `annotationText` 经过：

1. `text.normalize('NFKC')` — 分解 Unicode 连字（`ﬁ` → `fi` 等）
2. `text.replace(/\n/g, ' ')` — 将 PDF 换行折叠为空格
3. `text.replace(/ {2,}/g, ' ').trim()` — 规范化空白

### 标注注释字段

标注注释是一个 `contenteditable` div，带有 `aria-label="Annotation comment"`，
位于由 `[data-sidebar-annotation-id="${key}"]` 标识的侧边栏卡片内。
只使用 `.focus()` 聚焦——从 chrome 上下文调用 `.click()` 会创建特权
`MouseEvent`，content 代码无法读取，导致安全包装器错误。

---

## 许可证

[GNU Affero General Public License v3.0](LICENSE)（AGPL-3.0）。
